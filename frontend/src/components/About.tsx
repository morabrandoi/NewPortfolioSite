import * as React from "react"
import {
  Heading,
} from "@chakra-ui/react"

type AboutProps = {

}

export default function About(props: AboutProps) {

  return (
    <Heading>
      {"<About>"}
    </Heading>
  );
}
